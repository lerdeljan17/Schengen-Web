export async function GET(request:Request){const u=new URL(request.url),lat=Number(u.searchParams.get('lat')),lon=Number(u.searchParams.get('lon'));if(!u.searchParams.has('lat')||!u.searchParams.has('lon')||!Number.isFinite(lat)||!Number.isFinite(lon)||Math.abs(lat)>90||Math.abs(lon)>180)return Response.json({error:'Invalid coordinates'},{status:400});try{const r=await fetch(`https://nominatim.openstreetmap.org/reverse?format=jsonv2&lat=${lat.toFixed(3)}&lon=${lon.toFixed(3)}&zoom=3`,{headers:{'User-Agent':'SchengenTrackerWeb/1.0 (https://github.com/lerdeljan17/Schengen)','Accept-Language':'en'},signal:AbortSignal.timeout(10000)});if(!r.ok)throw Error();const d=await r.json() as {address?:{country_code?:string;country?:string}};if(!d.address?.country_code)throw Error();return Response.json({code:d.address.country_code.toUpperCase(),name:d.address.country},{headers:{'Cache-Control':'private, max-age=900'}})}catch{return Response.json({error:'Country lookup unavailable'},{status:502})}}
